/* import React, { Component } from 'react';

export default class button extends Component {
  render() {
      if (correct-answer)
    return (
      <div>
        <button className="button-correct-answer" data-testid="correct-answer"></button>
      </div>
    )
      if (wrong-answer)
    return (
      <div>=
        <button className="button-wrong-answer" data-testid="wrong-answer-${index}"></button>
      </div>
    )
  }
} */

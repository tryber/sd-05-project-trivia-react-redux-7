// referência https://github.com/tryber/sd-03-project-trivia-react-redux-05/pull/10

import React, { Component } from 'react';

export default class CompBotton extends Component {
/*   botao() {
    if (correct-answer) {
      return (
        <div>
          <button className="buttonCorrectAnswer" data-testid="correct-answer">.</button>
        </div>
      );
    }
    return (
      <div>
        <button className="buttonWrongAnswer" data-testid="wrong-answer-${index}">.</button>
      </div>
    );
  } */
  render() {
    return (
      <div>
        this.botao
      </div>
    );
  }
}
